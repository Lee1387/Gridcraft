# Gridcraft

Gridcraft is a web application for modern versions of classic grid-based games, starting with titles like Tic-Tac-Toe and Four in a Row.

## Getting Started

```bash
pnpm install
pnpm dev
```

## Scripts

- `pnpm dev` starts the development server.
- `pnpm build` runs a type-check and builds for production.
- `pnpm lint` runs ESLint.
- `pnpm format` formats the codebase with Prettier.
- `pnpm format:check` checks formatting without changing files.
- `pnpm check` runs lint, format checks, and production build validation.
